{
  "name": "write-advisor-block",
  "hook_event": "PreToolUse",
  "tool": "Write",
  "category": "destructive",
  "gate": "pre-write-mcp-advisor",
  "description": "A new >=20-line utility in scripts/ that duplicates an existing MCP/Skill (pdf-parse) with no Justified: line must BLOCK (exit 2, VIBE Rule 24).",
  "stdin": {
    "session_id": "00000000-0000-4000-8000-0000000000d1",
    "cwd": "/workspace/repo",
    "hook_event_name": "PreToolUse",
    "tool_name": "Write",
    "tool_input": {
      "file_path": "scripts/pdf-extract-helper.mjs",
      "content": "// pdf text extraction helper\nimport fs from 'node:fs';\nimport path from 'node:path';\nconst pdfParse = require('pdf-parse');\n\nexport async function extractTextFromPdf(filePath) {\n  const buf = fs.readFileSync(filePath);\n  const data = await pdfParse(buf);\n  return data.text;\n}\n\nexport function listPdfs(dir) {\n  return fs.readdirSync(dir)\n    .filter((f) => f.toLowerCase().endsWith('.pdf'))\n    .map((f) => path.join(dir, f));\n}\n\nexport async function extractAll(dir) {\n  const out = {};\n  for (const p of listPdfs(dir)) {\n    out[p] = await extractTextFromPdf(p);\n  }\n  return out;\n}\n\nexport default { extractTextFromPdf, listPdfs, extractAll };\n"
    }
  },
  "stdin_b64": null,
  "expect": { "exit_code": 2, "stdout": "", "stderr_contains": "BLOCKED", "side_effects": [] },
  "provenance": "scripts/hooks/pre-write-mcp-advisor.ps1 -- guarded dir + >=20 non-blank lines + pdf-parse pattern, no Justified: (live Windows session 2026-06-13)."
}
